<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="gallery.imgur.com.imgurgallery">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".ImgurApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.main.MainActivity"
            android:configChanges="orientation|screenSize"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:configChanges="orientation|screenSize" android:name=".ui.about.AboutActivity" android:theme="@style/AppTheme.OtherScreens" android:label="@string/title_about"/>
        <activity android:configChanges="orientation|screenSize" android:name=".ui.imageview.ImageViewActivity" android:theme="@style/AppTheme.OtherScreens" android:label="@string/title_image_view" />
        <activity android:configChanges="orientation|screenSize" android:theme="@style/AppTheme.SettingsScreens"
            android:name=".ui.settings.SettingsActivity"
            android:label="@string/title_activity_settings"></activity>
    </application>

</manifest>